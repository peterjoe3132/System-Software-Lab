#include<stdio.h>
void main()
{
	int n,dist,head,temp,total=0,beg,end;
	printf("Enter the number of reads :");
	scanf("%d",&n);
	printf("Enter the current position:");
	scanf("%d",&head);
	printf("Enter the beginning of the file :");
	scanf("%d",beg);
	printf("Enter the end of the file :");
	scanf("%d",end);
	
	return 0;

}